# Requirements

- REQ-HITL-020: Chat renders Approve and Reject when a tool is parked.
- REQ-HITL-021: Those buttons call `POST /api/approvals/{id}/decision`.
- REQ-HITL-022: The card shows the decision result and disables the buttons.
