# Design

1024 tokens stopped the 180s timeout but cannot emit react-loop.ps1. 8192 plus a 600s read timeout is enough for one write_project_file. agentic-test has no git repo; skip_commit prevents a git loop from eating the 10-turn budget.
