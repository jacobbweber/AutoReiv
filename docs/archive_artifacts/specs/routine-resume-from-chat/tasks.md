# Tasks

- [x] Store `routine_id` on `pending_approvals` and pass it from the routine executor
- [x] Filter GET pending by `agent_id`; label routine parks
- [x] `run_turn` resume-from-history without a new USER message
- [x] After routine Approve or Reject, resume that session when Chat is on a different session
- [x] Chat loads pending cards on agent/session switch and a coarse poll
- [x] Tests and CHANGELOG
