# Nested Write Budget

## REQ-ORCH-030
Nested complete max_tokens is 8192. Ollama HTTP read timeout is 600s so a real script write can finish.

## REQ-SDLC-073
git_status and git_commit on a non-repo return skip_commit=true with an instruction to skip commit and set In Review.
