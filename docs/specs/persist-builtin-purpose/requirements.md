# Persist Builtin Agent Purpose

## REQ-FORGE-020
A Forge save of a builtin agent's purpose must persist across GET. Coding saved as `general` must return `general`.

## REQ-FORGE-021
Invalid purpose values on the override are ignored. The builtin baseline purpose remains.
