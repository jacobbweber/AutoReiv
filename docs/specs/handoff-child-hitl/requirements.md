# Requirements

- REQ-HITL-023: A parked tool inside `run_turn` ends the turn with `approval_required`.
- REQ-HITL-024: A handoff that parks a child tool emits `approval_required` on the parent stream.
