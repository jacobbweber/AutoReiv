diff --git a/CHANGELOG.md b/CHANGELOG.md
index 7ebd7e5..2351cb6 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -4,6 +4,7 @@
 - CARD-313: Settings collapsed sections (Providers / Data / Preferences / Connections) + honest `POST /api/data-dir/migrate` (copy, validate, `*_backup_<ts>`, persist `AUTOREIV_DATA_DIR`)
 
 ### Fixed
+- CARD-314: Train Specialist modal scrolls (max-h + body overflow); Factory studio min-h-0 + full desktop window (not toast); Agents deep-link still scopes agent queue
 - CARD-312: Observe expand sections scroll with the studio panel (phone + desktop)
 
 
diff --git a/src/web/static/modules/studios/factory.js b/src/web/static/modules/studios/factory.js
index da6fe3d..944f96b 100644
--- a/src/web/static/modules/studios/factory.js
+++ b/src/web/static/modules/studios/factory.js
@@ -1343,8 +1343,16 @@ export function initFactoryStudio(state, callbacks = {}) {
   }
 
   // Global helper bridge
+  // CARD-314: always open full Factory Studio window (switchTab/desktop host), not a toast/chip.
   if (typeof window !== 'undefined') {
     window.openFactoryStudioForAgent = (agentId) => {
+      if (typeof callbacks.openFactoryStudio === 'function') {
+        callbacks.openFactoryStudio(agentId);
+        return;
+      }
+      if (typeof callbacks.switchTab === 'function') {
+        callbacks.switchTab('factory');
+      }
       switchSubView('runs');
       setAgentScope(agentId);
       loadTrainingRuns();
diff --git a/src/web/static/modules/ui/agent-desktop.js b/src/web/static/modules/ui/agent-desktop.js
index ce1bfd7..8edfe8b 100644
--- a/src/web/static/modules/ui/agent-desktop.js
+++ b/src/web/static/modules/ui/agent-desktop.js
@@ -22,7 +22,7 @@ export const DOCK_LAUNCHERS = /** @type {DockLauncher[]} */ ([
   { id: 'dock-wiki', tab: 'wiki', label: 'Wiki', icon: 'book-marked', subtitle: 'Document repository', defaultSize: { w: 780, h: 560 } },
   { id: 'dock-projects', tab: 'projects', label: 'Projects', icon: 'folders', subtitle: 'Workspaces', defaultSize: { w: 760, h: 540 } },
   { id: 'dock-agents', tab: 'agents', label: 'Agents', icon: 'users', subtitle: 'Forge / fleet', defaultSize: { w: 820, h: 580 } },
-  { id: 'dock-factory', tab: 'factory', label: 'Factory', icon: 'flask-conical', subtitle: 'Training lab', defaultSize: { w: 860, h: 600 } },
+  { id: 'dock-factory', tab: 'factory', label: 'Factory', icon: 'flask-conical', subtitle: 'Training lab', defaultSize: { w: 960, h: 680 } },
   { id: 'dock-routines', tab: 'routines', label: 'Routines', icon: 'clock', subtitle: 'Schedules', defaultSize: { w: 700, h: 520 } },
   { id: 'dock-observability', tab: 'observability', label: 'Observe', icon: 'bar-chart-3', subtitle: 'Telemetry', defaultSize: { w: 760, h: 540 } },
   { id: 'dock-settings', tab: 'settings', label: 'Settings', icon: 'settings', subtitle: 'Providers', defaultSize: { w: 720, h: 540 } },
@@ -914,6 +914,26 @@ export function initAgentDesktop(opts = {}) {
 
     root.classList.add('desktop-has-windows');
 
+    // CARD-314: Factory deep-link / dock open must present as a full studio window, not a toast-sized chip.
+    if (tab === 'factory' && win && !win.maximized) {
+      const launcher = launcherForTab('factory');
+      const def = (launcher && launcher.defaultSize) || { w: 960, h: 680 };
+      const vp = viewportSize();
+      const tooSmall = (win.rect.w || 0) < 560 || (win.rect.h || 0) < 420;
+      if (tooSmall) {
+        win.rect = clampWindowRect(
+          {
+            x: win.rect.x,
+            y: win.rect.y,
+            w: Math.min(def.w, vp.width - 32),
+            h: Math.min(def.h, vp.height - vp.dockH - 24),
+          },
+          vp,
+        );
+        applyRect(win);
+      }
+    }
+
     if (tab !== 'sessions' && typeof switchTab === 'function') {
       switchTab(tab);
     }
diff --git a/src/web/templates/index.html b/src/web/templates/index.html
index 75c73ce..9188b5f 100644
--- a/src/web/templates/index.html
+++ b/src/web/templates/index.html
@@ -881,6 +881,28 @@
       overscroll-behavior: contain;
       touch-action: pan-y;
     }
+
+    /* CARD-314: Factory fills hosted desktop window (full studio, not toast); outer stays overflow-hidden for runs split pane; pipeline/runs scroll inside. */
+    #view-factory,
+    #factoryStudio {
+      min-height: 0;
+    }
+    body.radical-desktop-demo #view-factory.desktop-view-hosted,
+    body.radical-desktop-demo #view-factory.desktop-view-hosted #factoryStudio {
+      min-height: 0 !important;
+      height: 100% !important;
+      max-height: 100% !important;
+      overflow: hidden !important;
+    }
+    #factoryPipelineView {
+      min-height: 0;
+      -webkit-overflow-scrolling: touch;
+      overscroll-behavior: contain;
+      touch-action: pan-y;
+    }
+    #factoryRunsView {
+      min-height: 0;
+    }
     #view-settings details.settings-section {
       border-radius: 0.75rem;
       border: 1px solid rgba(255, 255, 255, 0.06);
@@ -4129,8 +4151,8 @@
       </section>
 
       <!-- ==================== VIEW 9: FACTORY STUDIO [CARD-195] ==================== -->
-      <section id="view-factory" role="tabpanel" aria-labelledby="tab-factory" aria-hidden="true" class="tab-view flex-1 flex flex-col h-full overflow-hidden relative hidden">
-        <div id="factoryStudio" class="flex-1 flex flex-col h-full overflow-hidden">
+      <section id="view-factory" role="tabpanel" aria-labelledby="tab-factory" aria-hidden="true" class="tab-view flex-1 flex flex-col h-full min-h-0 overflow-hidden relative hidden" data-card="314">
+        <div id="factoryStudio" class="flex-1 flex flex-col h-full min-h-0 overflow-hidden">
           <!-- Factory Studio Top Navigation Bar -->
         <div class="px-4 md:px-6 py-3 bg-[#0e1015]/90 border-b border-white/[0.06] flex flex-wrap items-center justify-between gap-3 flex-shrink-0 backdrop-blur-md z-10">
           <div class="flex items-center space-x-3">
@@ -4185,7 +4207,7 @@
         </div>
 
         <!-- ==================== SUB-VIEW 1: PIPELINE & PHASE PROMPTS ==================== -->
-        <div id="factoryPipelineView" class="flex-1 overflow-y-auto p-4 md:p-6 space-y-6">
+        <div id="factoryPipelineView" class="flex-1 min-h-0 overflow-y-auto overscroll-contain touch-pan-y p-4 md:p-6 space-y-6">
           <!-- Flowchart Header & 8-Stage Stepper Container -->
           <div class="bg-[#0e1015]/90 border border-white/[0.06] rounded-2xl p-5 space-y-4 shadow-xl backdrop-blur-sm">
             <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2 border-b border-white/[0.06] pb-3">
@@ -4269,7 +4291,7 @@
         </div>
 
         <!-- ==================== SUB-VIEW 2: TRAINING RUNS & LIVE MONITOR ==================== -->
-        <div id="factoryRunsView" class="flex-1 flex flex-col lg:flex-row h-full overflow-hidden hidden">
+        <div id="factoryRunsView" class="flex-1 flex flex-col lg:flex-row h-full min-h-0 overflow-hidden hidden">
           <!-- Left Pane: Runs List & Filter -->
           <div id="factoryRunsListPane" class="w-full lg:w-80 xl:w-96 bg-[#0e1015]/70 border-b lg:border-b-0 lg:border-r border-white/[0.06] flex flex-col flex-shrink-0 h-full overflow-hidden">
             <!-- Filter Header -->
@@ -4642,15 +4664,16 @@
 
       <!-- ==================== TRAIN AGENT SOCRATIC HANDSHAKE MODAL [CARD-159, REQ-FACT-005] ==================== -->
       <div id="trainAgentHandshakeModal" role="dialog" aria-modal="true" aria-labelledby="trainAgentModalTitle" class="fixed inset-0 z-50 hidden bg-[#08090c]/85 backdrop-blur-md flex items-center justify-center p-4">
-        <div class="bg-[#0e1015] border border-white/[0.08] rounded-2xl w-full max-w-xl shadow-2xl overflow-hidden flex flex-col">
-          <div class="px-6 py-4 border-b border-white/[0.06] flex items-center justify-between bg-[#0e1015]/90">
+        <!-- CARD-314: Factory train modal scrolls -->
+        <div class="bg-[#0e1015] border border-white/[0.08] rounded-2xl w-full max-w-xl max-h-[90vh] min-h-0 shadow-2xl overflow-hidden flex flex-col">
+          <div class="px-6 py-4 border-b border-white/[0.06] flex items-center justify-between bg-[#0e1015]/90 flex-shrink-0">
             <h3 id="trainAgentModalTitle" class="font-bold text-sm text-emerald-300 flex items-center space-x-2">
               <i data-lucide="cpu" class="w-4 h-4 text-emerald-400"></i>
               <span>Train Specialist Agent (Lab Loop)</span>
             </h3>
             <button type="button" id="closeTrainAgentModalBtn" class="text-slate-400 hover:text-white p-1 rounded-lg"><i data-lucide="x" class="w-4 h-4"></i></button>
           </div>
-          <div class="p-6 space-y-4 text-xs text-slate-300">
+          <div class="p-6 space-y-4 text-xs text-slate-300 flex-1 min-h-0 overflow-y-auto overscroll-contain touch-pan-y">
             <p class="text-slate-400">AutoReiv will run the Agent Training Factory (Ground to Promote) to research the role, ground in Wiki, author tools, and verify them in an isolated sandbox.</p>
             
             <!-- Locked Target Agent & Verified Pack Banner [CARD-195, REQ-FACT-043] -->
@@ -4739,7 +4762,7 @@
               </label>
             </div>
           </div>
-          <div class="px-6 py-3.5 border-t border-white/[0.06] bg-[#0e1015]/90 flex items-center justify-end space-x-2">
+          <div class="px-6 py-3.5 border-t border-white/[0.06] bg-[#0e1015]/90 flex items-center justify-end space-x-2 flex-shrink-0">
             <button type="button" id="cancelTrainAgentBtn" class="px-3.5 py-1.5 bg-[#13161f] hover:bg-white/[0.06] text-slate-300 rounded-lg text-xs font-semibold transition">Cancel</button>
             <button type="button" id="startTrainAgentBtn" class="px-4 py-1.5 bg-emerald-600 hover:bg-emerald-500 text-white rounded-lg text-xs font-semibold shadow-sm transition flex items-center space-x-1.5">
               <i data-lucide="play" class="w-3.5 h-3.5"></i>
@@ -5000,8 +5023,9 @@
 
   <!-- Lab Artifact Preview Modal [CARD-171] -->
   <div id="labArtifactPreviewModal" data-testid="lab-artifact-preview-modal" class="hidden fixed inset-0 z-[60] flex items-center justify-center p-4 bg-[#08090c]/85 backdrop-blur-md" role="dialog" aria-modal="true" aria-labelledby="labArtifactPreviewTitle">
-    <div class="bg-[#0e1015] border border-white/[0.08] rounded-2xl max-w-3xl w-full max-h-[85vh] shadow-2xl flex flex-col overflow-hidden">
-      <div class="px-5 py-4 border-b border-white/[0.06] flex items-center justify-between">
+    <!-- CARD-314: lab artifact preview modal scrolls -->
+    <div class="bg-[#0e1015] border border-white/[0.08] rounded-2xl max-w-3xl w-full max-h-[85vh] min-h-0 shadow-2xl flex flex-col overflow-hidden">
+      <div class="px-5 py-4 border-b border-white/[0.06] flex items-center justify-between flex-shrink-0">
         <div class="flex items-center space-x-2 min-w-0">
           <i data-lucide="file-search" class="w-4 h-4 text-amber-400 flex-shrink-0"></i>
           <h3 id="labArtifactPreviewTitle" class="text-sm font-semibold text-white truncate">Artifact</h3>
@@ -5010,17 +5034,17 @@
           <i data-lucide="x" class="w-4 h-4"></i>
         </button>
       </div>
-      <div class="px-5 py-3 border-b border-white/[0.06] space-y-2 bg-[#08090c]/40">
+      <div class="px-5 py-3 border-b border-white/[0.06] space-y-2 bg-[#08090c]/40 flex-shrink-0">
         <p id="labArtifactPreviewNote" class="text-[11px] text-amber-200/90">Pre-promote: content is from the job packet (not yet written under packs/).</p>
         <div>
           <div class="text-[10px] uppercase tracking-wider text-slate-500 mb-1">Expected deploy paths</div>
           <ul id="labArtifactPreviewPaths" class="space-y-1"></ul>
         </div>
       </div>
-      <div class="flex-1 overflow-auto p-5 bg-[#08090c]">
+      <div class="flex-1 min-h-0 overflow-y-auto overscroll-contain touch-pan-y p-5 bg-[#08090c]">
         <pre id="labArtifactPreviewBody" data-testid="lab-artifact-preview-body" class="text-[11px] font-mono text-slate-200 whitespace-pre-wrap break-words bg-[#08090c] border border-white/[0.06] rounded-xl p-3 max-h-[50vh] overflow-auto"></pre>
       </div>
-      <div class="px-5 py-3 border-t border-white/[0.06] bg-[#0e1015]/90 flex justify-end">
+      <div class="px-5 py-3 border-t border-white/[0.06] bg-[#0e1015]/90 flex justify-end flex-shrink-0">
         <button type="button" id="closeLabArtifactPreviewFooterBtn" class="px-3 py-1.5 bg-[#13161f] hover:bg-white/[0.06] text-slate-300 rounded-lg text-xs font-medium transition">Close</button>
       </div>
     </div>
@@ -5229,7 +5253,7 @@
   <!-- Fixed Toast Container [REQ-TOAST-001] -->
   <div id="toastContainer" role="region" aria-label="Notifications" aria-live="polite" aria-atomic="true" class="fixed bottom-4 right-4 z-50 flex flex-col space-y-2 pointer-events-none max-w-sm w-full px-4 sm:px-0"></div>
 
-  <script type="module" src="/static/app.js?v=2.0.49"></script>
+  <script type="module" src="/static/app.js?v=2.0.50"></script>
 </body>
 </html>
