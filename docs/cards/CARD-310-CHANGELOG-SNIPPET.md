# CARD-310 CHANGELOG [Unreleased] snippet

### Added
- **Routines structured schedule + full agent pickers [CARD-310]**: Calendar-feel months/weekdays/DOM/time + every-N-weeks with anchor date; durable metadata schedule_rule with ScheduleType.STRUCTURED; matcher computes next fire (biweekly); preview endpoint; cron when representable; both agent dropdowns from GET /api/agents.
